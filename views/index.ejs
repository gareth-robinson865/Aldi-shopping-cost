<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs') %> <!--head partial-->
</head>
<body>
    <%- include('./partials/nav.ejs') %> <!--nav partial-->
    
    <div class="list-content">
        <h2>All food</h2>
        <% if(foods.length > 0) { %>
            <% foods.forEach(food => { %>
                <a class="single" href="details.ejs/<%= food._id %>">
                    <h3 class="name"><%= food.name %></h3>
                    <p class="price">£<%= food.price %></p>
                    <p class="aisle"><%= food.aisle %></p>
                </a>
            <% }) %>
        <% } else { %>
            <p>There are no foods to display</p>
        <% } %>
    </div>

    <%- include('./partials/footer.ejs') %> <!--footer partial-->
</body>
</html>